@model CodeShield.Models.LoginViewModel
@{
    ViewData["Title"] = "Log In";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="cs-card p-4 p-sm-5">
                <div class="text-center mb-4">
                    <img src="~/images/logo.jpg" alt="Valkyrie Logo" class="form-card-logo mb-2" />
                    <h2 class="fw-bold text-dark">Welcome Back</h2>
                    <p class="text-muted small">Log in to monitor your codebase security</p>
                </div>
                
                <form asp-controller="Account" asp-action="Login" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post" novalidate>
                    @if (!ViewData.ModelState.IsValid && ViewData.ModelState[string.Empty]?.Errors.Count > 0)
                    {
                        <div class="alert cs-alert-danger p-3 mb-4 rounded-3" role="alert">
                            <div asp-validation-summary="ModelOnly" class="mb-0"></div>
                        </div>
                    }

                    <div class="mb-3">
                        <label asp-for="Username" class="form-label fw-semibold text-secondary small"></label>
                        <input asp-for="Username" class="form-control cs-input" placeholder="Enter username" aria-required="true" />
                        <span asp-validation-for="Username" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-3">
                        <div class="d-flex justify-content-between align-items-center mb-1">
                            <label asp-for="Password" class="form-label fw-semibold text-secondary small mb-0"></label>
                            <a href="#" class="text-decoration-none small text-primary fw-medium" data-bs-toggle="modal" data-bs-target="#forgotPasswordModal">Forgot password?</a>
                        </div>
                        <div class="cs-input-group">
                            <input asp-for="Password" type="password" id="passwordInput" class="form-control cs-input" placeholder="••••••••" aria-required="true" />
                            <button type="button" class="password-toggle" id="togglePasswordBtn" aria-label="Toggle password visibility">
                                <span id="togglePasswordIcon">👁️</span>
                            </button>
                        </div>
                        <span asp-validation-for="Password" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="form-check mb-4">
                        <input class="form-check-input" asp-for="RememberMe" />
                        <label asp-for="RememberMe" class="form-check-label small text-muted">
                            Remember my login
                        </label>
                    </div>

                    <button class="w-100 cs-btn cs-btn-primary mb-3 text-white" type="submit">Log In</button>
                    
                    <div class="text-center mt-3">
                        <p class="text-muted small mb-0">
                            New to Valkyrie? <a asp-controller="Account" asp-action="Register" class="text-decoration-none fw-semibold text-primary">Create an account</a>.
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Forgot Password Info Modal -->
<div class="modal fade" id="forgotPasswordModal" tabindex="-1" aria-labelledby="forgotPasswordModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content cs-modal-content">
            <div class="modal-header cs-modal-header d-flex align-items-center justify-content-between">
                <h5 class="modal-title fw-bold text-dark d-flex align-items-center gap-2" id="forgotPasswordModalLabel">
                    <span>🔑</span> Password Recovery
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body cs-modal-body text-secondary">
                <div class="text-center mb-4">
                    <span class="fs-1">🌐</span>
                    <h6 class="fw-bold mt-2 text-dark">Local Sandbox Environment</h6>
                </div>
                <p class="small">
                    This instance of <strong>Valkyrie</strong> is running as a local security tool with no connection to external SMTP/Gmail or SMS services.
                </p>
                <p class="small mb-0">
                    If you've forgotten your password, you can:
                </p>
                <ul class="small mt-2 ps-3">
                    <li class="mb-1"><strong>Register a new username</strong> to continue scanning public repositories.</li>
                    <li>Contact your local system administrator to reset or re-seed the credentials from the database.</li>
                </ul>
            </div>
            <div class="modal-footer cs-modal-footer">
                <button type="button" class="btn btn-secondary cs-btn w-100" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function () {
            $('#togglePasswordBtn').click(function () {
                const passwordInput = $('#passwordInput');
                const icon = $('#togglePasswordIcon');
                
                if (passwordInput.attr('type') === 'password') {
                    passwordInput.attr('type', 'text');
                    icon.text('🙈');
                } else {
                    passwordInput.attr('type', 'password');
                    icon.text('👁️');
                }
            });
        });
    </script>
}
