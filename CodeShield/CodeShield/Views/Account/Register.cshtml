@model CodeShield.Models.RegisterViewModel
@{
    ViewData["Title"] = "Register";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="cs-card p-4 p-sm-5">
                <div class="text-center mb-4">
                    <img src="~/images/logo.jpg" alt="Valkyrie Logo" class="form-card-logo mb-2" />
                    <h2 class="fw-bold text-dark">Create Account</h2>
                    <p class="text-muted small">Protect your repository against safety risks</p>
                </div>
                
                <form asp-controller="Account" asp-action="Register" method="post" novalidate>
                    @if (!ViewData.ModelState.IsValid && ViewData.ModelState[string.Empty]?.Errors.Count > 0)
                    {
                        <div class="alert cs-alert-danger p-3 mb-4 rounded-3" role="alert">
                            <div asp-validation-summary="ModelOnly" class="mb-0"></div>
                        </div>
                    }

                    <div class="mb-3">
                        <label asp-for="Username" class="form-label fw-semibold text-secondary small"></label>
                        <input asp-for="Username" class="form-control cs-input" placeholder="Choose a username" aria-required="true" />
                        <span asp-validation-for="Username" class="text-danger small mt-1 d-block"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Password" class="form-label fw-semibold text-secondary small"></label>
                        <div class="cs-input-group">
                            <input asp-for="Password" type="password" id="passwordInput" class="form-control cs-input" placeholder="••••••••" aria-required="true" />
                            <button type="button" class="password-toggle" id="togglePasswordBtn" aria-label="Toggle password visibility">
                                <span id="togglePasswordIcon">👁️</span>
                            </button>
                        </div>
                        <span asp-validation-for="Password" class="text-danger small mt-1 d-block"></span>
                        
                        <div class="password-requirements" id="passwordChecklist">
                            <div class="requirement-item" id="reqLength">
                                <span class="req-icon" id="reqLengthIcon">✕</span>
                                <span>At least 8 characters</span>
                            </div>
                        </div>
                    </div>

                    <div class="mb-4">
                        <label asp-for="ConfirmPassword" class="form-label fw-semibold text-secondary small"></label>
                        <div class="cs-input-group">
                            <input asp-for="ConfirmPassword" type="password" id="confirmPasswordInput" class="form-control cs-input" placeholder="••••••••" aria-required="true" />
                            <button type="button" class="password-toggle" id="toggleConfirmPasswordBtn" aria-label="Toggle password visibility">
                                <span id="toggleConfirmPasswordIcon">👁️</span>
                            </button>
                        </div>
                        <span asp-validation-for="ConfirmPassword" class="text-danger small mt-1 d-block"></span>
                        
                        <div class="password-requirements d-none" id="confirmPasswordChecklist">
                            <div class="requirement-item" id="reqMatch">
                                <span class="req-icon" id="reqMatchIcon">✕</span>
                                <span>Passwords match</span>
                            </div>
                        </div>
                    </div>

                    <button class="w-100 cs-btn cs-btn-primary mb-3 text-white" type="submit">Create Account</button>
                    
                    <div class="text-center mt-3">
                        <p class="text-muted small mb-0">
                            Already have an account? <a asp-controller="Account" asp-action="Login" class="text-decoration-none fw-semibold text-primary">Log in instead</a>.
                        </p>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        $(document).ready(function () {
            // Toggle password visibility
            $('#togglePasswordBtn').click(function () {
                const passwordInput = $('#passwordInput');
                const icon = $('#togglePasswordIcon');
                if (passwordInput.attr('type') === 'password') {
                    passwordInput.attr('type', 'text');
                    icon.text('🙈');
                } else {
                    passwordInput.attr('type', 'password');
                    icon.text('👁️');
                }
            });

            $('#toggleConfirmPasswordBtn').click(function () {
                const confirmInput = $('#confirmPasswordInput');
                const icon = $('#toggleConfirmPasswordIcon');
                if (confirmInput.attr('type') === 'password') {
                    confirmInput.attr('type', 'text');
                    icon.text('🙈');
                } else {
                    confirmInput.attr('type', 'password');
                    icon.text('👁️');
                }
            });

            // Real-time password length validation
            const passwordInput = $('#passwordInput');
            const reqLength = $('#reqLength');
            const reqLengthIcon = $('#reqLengthIcon');

            passwordInput.on('input', function () {
                const val = $(this).val();
                if (val.length >= 8) {
                    reqLength.addClass('valid');
                    reqLengthIcon.text('✓');
                } else {
                    reqLength.removeClass('valid');
                    reqLengthIcon.text('✕');
                }
                checkPasswordsMatch();
            });

            // Real-time matching validation
            const confirmInput = $('#confirmPasswordInput');
            const confirmChecklist = $('#confirmPasswordChecklist');
            const reqMatch = $('#reqMatch');
            const reqMatchIcon = $('#reqMatchIcon');

            confirmInput.on('input', function () {
                if ($(this).val().length > 0) {
                    confirmChecklist.removeClass('d-none');
                } else {
                    confirmChecklist.addClass('d-none');
                }
                checkPasswordsMatch();
            });

            function checkPasswordsMatch() {
                const pass = passwordInput.val();
                const confirmPass = confirmInput.val();
                if (confirmPass.length > 0) {
                    if (pass === confirmPass) {
                        reqMatch.addClass('valid');
                        reqMatchIcon.text('✓');
                    } else {
                        reqMatch.removeClass('valid');
                        reqMatchIcon.text('✕');
                    }
                }
            }
        });
    </script>
}
